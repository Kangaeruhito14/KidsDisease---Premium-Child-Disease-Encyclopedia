{
  "name": "kidsdisease",
  "description": "Kids Disease — child disease encyclopedia for parents and caregivers. Educational reference only; not medical advice.",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "build:legacy-db": "node build_db.js",
    "deploy": "npm run build && wrangler pages deploy dist --project-name kidsdisease --branch main --commit-dirty=true"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.6",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.0",
    "wrangler": "^4.100.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
